name: rag-embedding
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - cxx-compiler
  - cmake
  - setuptools
  - ipykernel
  - pip:
      - python-dotenv
      - numpy>=2.0.0
      - torch>=2.4.1
      - torchvision>=0.19.1
      - streamlit
      - sentence-transformers
      - requests
      - pandas
      - ipywidgets 
      - hf_transfer
